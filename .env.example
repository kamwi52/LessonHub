# Database (PostgreSQL connection string)
DATABASE_URL=postgresql://user:password@host:5432/lessonshub

# Authentication
JWT_SECRET=your_super_secret_jwt_key_change_in_production
JWT_EXPIRY=7d

# Note: API routes are now built into the Next.js app at /api/*
# No separate backend URL needed
